#include <iostream>
#include <string>
#include <vector>
#include <algorithm>
#include <cmath>

int main()
{
    std::cout << "Please enter yourt first name (followed by enter)\n";
    std::string first_name; // first_name is initialized with the string type
    std::cin >> first_name; // read characters into first name variable
    std::cout << first_name << "!\n";
};